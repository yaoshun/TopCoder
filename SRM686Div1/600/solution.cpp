#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <unordered_set>
#include <queue>
#include <stack>
using namespace std;

class CyclesNumber {
public:
    vector<int> getExpectation(vector<int> n, vector<int> m) {
        int s = n.size();
        for (int i = 0; i < s; i++) {
            // calculate for n[i] and m[i].
        }
    }
};
